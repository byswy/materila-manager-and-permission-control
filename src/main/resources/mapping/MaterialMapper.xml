<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.btp.rwj.mapper.MaterialMapper">
    <select id="getAll" resultType="com.btp.rwj.entity.Material">
        select *
        from rwj_material
        where is_deleted = 0
    </select>
    <select id="getByCode" resultType="com.btp.rwj.entity.Material">
        select *
        from rwj_material
        where material_code = #{code}
          and is_deleted = 0
    </select>
    <select id="getById" resultType="com.btp.rwj.entity.Material">
        select *
        from rwj_material
        where id = #{id}
          and is_deleted = 0
    </select>
    <select id="getTypes" resultType="java.lang.String">
        select material_type
        from rwj_material
        where is_deleted = 0
    </select>
    <select id="getIds" resultType="java.lang.Integer">
        select id
        from rwj_material
        where is_deleted = 0
    </select>
    <update id="deleteById">
        update rwj_material
        set is_deleted=1
        where id = #{id}
    </update>
    <update id="delete">
        update rwj_material
        set is_deleted=0
        where material_code = #{materialCode}
    </update>
    <insert id="save">
        insert into rwj_material (material_code, material_name, material_type, material_num, material_price)
        values (#{materialCode}, #{materialName}, #{materialType}, #{materialNum}, #{materialPrice})
    </insert>
    <update id="update">
        update rwj_material
        set material_code  = #{materialCode},
            material_name  = #{materialName},
            material_type  = #{materialType},
            material_num   = #{materialNum},
            material_price = #{materialPrice}
        where id = #{id}
    </update>
</mapper>